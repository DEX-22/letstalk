Rules

Use TypeScript Strict.

Never use any.

Prefer interfaces.

Use Composition API.

One component, one responsibility.

No duplicated logic.

Use reusable composables.

Repositories only access Supabase.

Services contain business logic.

No magic strings.

Use constants.

Use enums.

Every form must use Zod.

Every feature must be testable.
